import { connect } from 'react-redux'
import SearchTools from './search_tools';

const msp = state => {
    return ({

    })
}

const mdp = dispatch => {
    return ({

    })
}

export default connect(msp, mdp)(SearchTools);